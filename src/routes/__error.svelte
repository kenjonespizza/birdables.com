<script context="module">
	export function load({ error, status }) {
		return {
			props: {
				title: `${status}: ${error.message}`,
        status,
        error,
			}
		};
	}
</script>

<script>
	export let status;
	export let error;
</script>

<!-- This example requires Tailwind CSS v2.0+ -->
<div class="bg-white h-[calc(100vh-328px)] px-4 py-16 sm:px-6 sm:py-24 md:grid md:place-items-center lg:px-8">
  <div class="max-w-max mx-auto">
    <main class="sm:flex">
      <p class="text-4xl font-extrabold text-beak-600 sm:text-7xl">{status}</p>
      <div class="sm:ml-6">
        <div class="sm:border-l sm:border-gray-200 sm:pl-6">
          <h1 class="text-4xl font-extrabold text-gray-900 tracking-tight sm:text-5xl">{status === 404 ? "This page already migrated!" : error.message}</h1>
          {#if status === 404}
            <p class="mt-2 text-base text-gray-500">Please check the URL in the address bar and try again.</p>
          {:else if status === 500}
            <p class="mt-2 text-base text-gray-500">There was an error generating this page.  You can try to refresh, but your best bet is to <a href="/">migrate back down south</a></p>
          {:else}
            <p class="mt-2 text-base text-gray-500">There was an error generating this page.  You can try to refresh, but your best bet is to <a href="/">migrate back down south</a></p>
          {/if}
        </div>
        <div class="mt-10 flex space-x-3 sm:border-l sm:border-transparent sm:pl-6">
          <a href="/" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-gray-900 hover:bg-beak-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-beak-500">
            Go back home
          </a>
          <!-- <a href="/" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-beak-700 bg-beak-100 hover:bg-beak-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-beak-500">
            Contact support
          </a> -->
        </div>
      </div>
    </main>
  </div>
</div>
